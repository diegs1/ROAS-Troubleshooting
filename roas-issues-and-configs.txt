1. Check Trunks (S1 to S2, S1 to Router)

	-	S1 to S2 native VLAN mismatch
	-	Untagged traffic on the trunk is misinterpreted.

			S1(config)# interface fa0/1
			S1(config-if)# switchport trunk native vlan 8


	-	S1 to router port is configured as an access port
	-	ROAS inter-VLAN routing fails.

			S1(config)# interface fa0/5
			S1(config-if)# switchport mode trunk
			S1(config-if)# switchport trunk encapsulation dot1q

	-	S1 to S2 trunk does not allow VLAN 3 traffic
	-	VLAN 3 interface on S2 is unreachable from S1, and vice versa.

			S1(config)# interface fa0/1
			S1(config-if)# switchport trunk allowed vlan add 3


2. Check Router Sub-interfaces

	-	g0/0/1.8 (VLAN 8) has no IP address → devices in native VLAN 8 cannot route.
	-	g0/0/1.4 (VLAN 4) uses the native keyword incorrectly.
	-	g0/0/1.3 and g0/0/1.13 are correct (VLAN tagging and IP addressing are proper).

		Fix / Configuration:

			R1(config)# interface g0/0/1
			R1(config)# shutdown
			R1(config)# no shutdown

			R1(config)# interface g0/0/1.3
			R1(config-subif)# encapsulation dot1Q 3
			R1(config-subif)# ip address 10.3.0.1 255.255.255.0

			R1(config)# interface g0/0/1.4
			R1(config-subif)# encapsulation dot1Q 4
			R1(config-subif)# ip address 10.4.0.1 255.255.255.0

			R1(config)# interface g0/0/1.8
			R1(config-subif)# encapsulation dot1Q 8
			R1(config-subif)# ip address 172.17.8.1 255.255.255.0

			R1(config)# interface g0/0/1.13
			R1(config-subif)# encapsulation dot1Q 13
			R1(config-subif)# ip address 10.13.0.1 255.255.255.0


3. Check Configured VLANs on Each Switch

	-	VLAN 13 is not configured on S1 → ports assigned to VLAN 13 are inactive.

			S1(config)# vlan 13
			S1(config-vlan)# name Maintenance

			S1(config)# interface fa0/3
			S1(config-if)# switchport mode access
			S1(config-if)# switchport access vlan 13